#ifndef CONSOLE_H
#define CONSOLE_H

/**
 * @brief Starts the console interface for the compiler.
 */
void go();

/**
 * @brief Tests the lexer with the given input.
 * @param input The input string to tokenize.
 */
void lexerTest(char* input);

#endif